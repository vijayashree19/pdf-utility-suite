# No environment variables are required for this module.
# pdfService.ts is 100% client-side (pdf-lib runs entirely in-browser) —
# no API keys, database credentials, or server endpoints are involved.
#
# This file exists as a placeholder/convention for future phases
# (e.g. if a future e-signature or cloud-sync feature requires config),
# so contributors always know to check here first before hardcoding anything.
